import 'package:flutter/material.dart';

class SlitherinScreen extends StatelessWidget {
  const SlitherinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Casa Slitherin')),
      body: Center(child: Text('Bienvenido a Slitherin')),
    );
  }
}
